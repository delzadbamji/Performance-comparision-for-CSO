D = 2
number_of_trials = 30

lower_bound = 0
upper_bound = 0

def set_lower_upper bound(lower_bound,upper_bound):
  lower_bound = lower_bound
  upper_bound = upper_bound
  
def get_lower_upper bound():
  return lower_bound,upper_bound
